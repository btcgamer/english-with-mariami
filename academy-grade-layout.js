/* English with Mariami — Academy grade layout compatibility stub. */
(function(){
  'use strict';
  /* Intentionally disabled on Academy. Grade cards must remain in their
     original layout and must not be moved, restyled, or used for navigation. */
})();
