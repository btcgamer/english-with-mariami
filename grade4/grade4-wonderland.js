/* GRADE 4 — WONDERLAND / INTERACTIVE MAGIC ENGINE */
(function(){'use strict';
const boot=()=>{if(document.body?.dataset.grade!=='4'||document.body.dataset.g4WonderBooted==='1')return;document.body.dataset.g4WonderBooted='1';
const realms=['wonder','library','clock','garden','mirror','sky','ocean','forest','castle','dream','quantum','crown'];
let root=null,label=null,cursor=null,last=0;
function spark(x,y){if(Date.now()-last<35)return;last=Date.now();const s=document.createElement('i');s.className='g4-spark';s.style.left=x+'px';s.style.top=y+'px';s.style.setProperty('--dx',(Math.random()*34-17)+'px');s.style.setProperty('--dy',(Math.random()*34-17)+'px');document.body.appendChild(s);setTimeout(()=>s.remove(),750)}
function burst(x,y){const b=document.createElement('i');b.className='g4-burst';b.style.left=x+'px';b.style.top=y+'px';document.body.appendChild(b);setTimeout(()=>b.remove(),850);for(let i=0;i<14;i++)spark(x+Math.random()*30-15,y+Math.random()*30-15)}
function ensureScene(){if(!document.body||document.body.dataset.grade!=='4')return;if(!document.querySelector('.g4-wonderland')){root=document.createElement('div');root.className='g4-wonderland';root.innerHTML='<div class="g4-starfield"></div><div class="g4-stars"></div><div class="g4-moon"></div><div class="g4-portal"></div><div class="g4-ribbon"></div><div class="g4-ribbon r2"></div>';document.body.prepend(root);for(let i=0;i<18;i++){const f=document.createElement('i');f.className='g4-float';f.style.left=(4+Math.random()*92)+'%';f.style.top=(10+Math.random()*82)+'%';f.style.animationDelay=(-Math.random()*7)+'s';f.style.opacity=(.25+Math.random()*.5).toFixed(2);root.appendChild(f)}}else root=document.querySelector('.g4-wonderland');if(!document.querySelector('.g4-wonder-label')){label=document.createElement('div');label.className='g4-wonder-label';label.textContent='WONDERLAND CORE • GRADE 4';document.body.append(label)}else label=document.querySelector('.g4-wonder-label');if(matchMedia('(pointer:fine)').matches&&!document.querySelector('.g4-magic-cursor')){cursor=document.createElement('div');cursor.className='g4-magic-cursor';document.body.append(cursor)}bindRealms()}
function bindRealms(){const world=[...document.querySelectorAll('.world,[data-m]')].filter((el,i,a)=>a.indexOf(el)===i).slice(0,12);world.forEach((el,i)=>{if(el.dataset.g4Bound==='1')return;el.dataset.g4Bound='1';el.classList.add('g4-realm');el.dataset.g4Realm=realms[i];el.addEventListener('pointerenter',()=>{if(label){label.textContent=`${el.dataset.g4Realm.toUpperCase()} REALM • UNLOCKED`}burst(el.getBoundingClientRect().left+el.offsetWidth/2,el.getBoundingClientRect().top+18)},{passive:true});el.addEventListener('click',()=>{const r=el.getBoundingClientRect();burst(r.left+r.width/2,r.top+r.height/2)},{passive:true})})}
if(matchMedia('(pointer:fine)').matches)document.addEventListener('pointermove',e=>{if(cursor){cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px'}spark(e.clientX,e.clientY)},{passive:true});document.addEventListener('click',e=>{const c=e.target.closest('.choice,.btn,[data-save]');if(c){const r=c.getBoundingClientRect();burst(r.left+r.width/2,r.top+r.height/2)}});ensureScene();new MutationObserver(()=>{if(!document.querySelector('.g4-wonderland'))ensureScene();else bindRealms()}).observe(document.body,{childList:true});
};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();})();
