/* REAL LESSON ENGINE V1 — database lesson -> learning steps -> scored completion */
(function(){'use strict';
const GRADE=3,WORLD_SIZE=5,client=window.__ENGLISH_MARIAMI_SUPABASE_CLIENT||window.supabaseClient,root=document.getElementById('missions');if(!root)return;
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
async function user(){if(!client)return null;const r=await client.auth.getUser();return r?.data?.user||null}
async function loadLesson(n){if(!client)return null;const r=await client.from('lessons').select('id,grade,lesson_number,title,topic,description,grammar_rule,grammar_examples,listening_text,speaking_phrases,reading_text,exercises').eq('grade',GRADE).eq('lesson_number',n).maybeSingle();return r.error?null:r.data}
async function completedIds(){const u=await user();if(!u)return new Set();const r=await client.from('lesson_progress').select('lesson_id,completed').eq('student_id',u.id).eq('grade',GRADE);return new Set((r.data||[]).filter(x=>x.completed===true).map(x=>String(x.lesson_id)))}
async function nextInWorld(world){const r=await client.from('lessons').select('id,lesson_number').eq('grade',GRADE).gte('lesson_number',world*WORLD_SIZE+1).lte('lesson_number',world*WORLD_SIZE+WORLD_SIZE).order('lesson_number');if(r.error)return null;const done=await completedIds();return (r.data||[]).find(x=>!done.has(String(x.id)))||null}
async function saveProgress(lesson,score){const u=await user();if(!u)return false;const r=await client.from('lesson_progress').upsert({student_id:u.id,grade:GRADE,lesson_id:lesson.id,completed:true,score:Math.max(0,Math.min(100,score)),completed_at:new Date().toISOString()},{onConflict:'student_id,lesson_id'});return !r.error}
function speak(t){if(!('speechSynthesis'in window))return;speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(t);u.lang='en-US';u.rate=.84;speechSynthesis.speak(u)}
function shell(html){const m=document.createElement('div');m.className='g4-vocab-modal real-lesson-modal';m.innerHTML=`<div class="g4-vocab-dialog" role="dialog" aria-modal="true">${html}</div>`;document.body.appendChild(m);m.addEventListener('click',e=>{if(e.target===m)m.remove()});m.querySelector('.real-close')?.addEventListener('click',()=>m.remove());return m}
async function openLesson(n){const l=await loadLesson(n);if(!l)return shell('<button class="g4-close real-close">×</button><h2>⚠️ Lesson unavailable</h2><p>We could not load this lesson from the learning database.</p>');const ex=Array.isArray(l.grammar_examples)?l.grammar_examples:[],sp=Array.isArray(l.speaking_phrases)?l.speaking_phrases:[],tasks=Array.isArray(l.exercises)?l.exercises:[];const m=shell(`<button class="g4-close real-close">×</button><div class="g4-modal-icon">🧠</div><span class="num">LESSON ${String(l.lesson_number).padStart(2,'0')}</span><h2>${esc(l.title)}</h2><p>${esc(l.topic||'English Mission')}</p><section class="real-step"><h3>📚 Learn</h3><p>${esc(l.description||'')}</p>${l.grammar_rule?`<p><strong>Grammar:</strong> ${esc(l.grammar_rule)}</p>`:''}${ex.map(x=>`<p>• ${esc(x)}</p>`).join('')}</section><section class="real-step"><h3>🗣️ Listen & Speak</h3><p>${esc(l.listening_text||'')}</p>${sp.map(x=>`<button class="real-speak" type="button" data-text="${esc(x)}">🔊 ${esc(x)}</button>`).join('')}</section><section class="real-step"><h3>📖 Read</h3><p>${esc(l.reading_text||'')}</p></section><section class="real-step"><h3>🎯 Mission Check</h3><p>Which rule is taught in this lesson?</p><div class="real-options"><button type="button" data-answer="correct">${esc(l.grammar_rule||'Review the lesson rule.')}</button><button type="button" data-answer="wrong">${esc(l.title)}</button><button type="button" data-answer="wrong">${esc(l.topic||'English')}</button></div><p class="real-feedback" aria-live="polite"></p></section><button class="g4-complete real-complete" type="button" disabled>✓ COMPLETE LESSON • +25 XP</button>`);m.querySelectorAll('.real-speak').forEach(b=>b.addEventListener('click',()=>speak(b.dataset.text)));let answered=false;m.querySelectorAll('[data-answer]').forEach(b=>b.addEventListener('click',()=>{if(answered)return;answered=true;if(b.dataset.answer==='correct'){m.querySelector('.real-feedback').textContent='⚡ Correct! Lesson power unlocked.';m.querySelector('.real-complete').disabled=false}else{m.querySelector('.real-feedback').textContent='Review the grammar rule above and try again.';answered=false}}));m.querySelector('.real-complete').addEventListener('click',async()=>{const ok=await saveProgress(l,100);if(!ok){m.querySelector('.real-feedback').textContent='⚠️ Progress could not be saved. Please reconnect and try again.';return}m.querySelector('.real-feedback').textContent='🏆 Saved to your learning progress!';m.querySelector('.real-complete').disabled=true;window.dispatchEvent(new CustomEvent('lessonprogressupdated',{detail:{grade:GRADE,lessonNumber:l.lesson_number}}));setTimeout(()=>m.remove(),500)});return m}
async function openWorld(i){const l=await nextInWorld(i);return openLesson(Number(l?.lesson_number||i*WORLD_SIZE+1))}
root.addEventListener('click',e=>{const c=e.target.closest('.mission');if(!c)return;const i=Number(c.dataset.i);if(!Number.isFinite(i))return;e.preventDefault();e.stopImmediatePropagation();if(c.dataset.worldState==='LOCKED')return;openWorld(i)},true);
const start=document.getElementById('start');if(start)start.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();(async()=>{const r=await client?.from('lessons').select('id,lesson_number').eq('grade',GRADE).order('lesson_number');if(!r?.data?.length)return;const done=await completedIds(),n=r.data.find(x=>!done.has(String(x.id)));if(n)openLesson(Number(n.lesson_number));})()},true);
window.G3RealLessonEngineV1={openLesson,openWorld,saveProgress};
})();
